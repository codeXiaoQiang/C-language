#include <stdio.h>

int main()
{
//    int num;
//    int value;
    // 同时定义多个变量
    // 注意点: 必须是相同类型的变量, 才可以这样写
//    int num,value;

    // 先定义再初始化
//    int num;
//    num = 10;

    // 定义的同时初始化
//    int num = 10;

    // 完全初始化
//    int num = 10,value = 998;
    // 部分初始化
    int num = 10,value;

    return 0;
}
